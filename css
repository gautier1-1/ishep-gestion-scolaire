body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 20px;
    background-color: #f5f5f5;  /* Exemple d'ajout */
}
/* Variables CSS */
:root {
  --primary-color: #2c3e50;
  --secondary-color: #3498db;
  --text-light: #fff;
}

.auth-container {
  max-width: 400px;
  margin: 2rem auto;
  padding: 2rem;
}

.auth-form input {
  transition: border 0.3s ease;
}

.auth-form input:focus {
  border-color: var(--secondary-color);
}
